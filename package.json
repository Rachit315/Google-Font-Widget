{
  "name": "stack-sans-card",
  "version": "1.0.0",
  "description": "Animated Stack Sans font card — Tailwind + Motion",
  "main": "index.js",
  "scripts": {
    "dev": "node server.js",
    "build": "tailwindcss -i src/input.css -o public/styles.css --minify",
    "watch": "tailwindcss -i src/input.css -o public/styles.css --watch",
    "start": "npm run build && node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "motion": "^13.2.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  },
  "private": true,
  "engines": {
    "node": ">=18"
  }
}
